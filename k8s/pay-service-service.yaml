apiVersion: v1
kind: Service
metadata:
  name: pay-service
  labels:
    app: pay-service
spec:
  type: ClusterIP
  selector:
    app: pay-service
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
