apiVersion: v1
kind: Service
metadata:
  name: ship-service
  labels:
    app: ship-service
spec:
  type: ClusterIP
  selector:
    app: ship-service
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
